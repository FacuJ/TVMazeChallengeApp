<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation_graph"
    app:startDestination="@id/seriesListFragment">

   <!-- <fragment
        android:id="@+id/loginFragment"
        android:name="com.facundojaton..login.LoginFragment"
        android:label="Login"
        tools:layout="@layout/fragment_login">
        <action
            android:id="@+id/action_login"
            app:destination="@id/seriesListFragment" />
    </fragment>-->

    <fragment
        android:id="@+id/seriesListFragment"
        android:name="com.facundojaton.tvmazechallenge.ui.series.SeriesListFragment"
        android:label="Series"
        tools:layout="@layout/fragment_series_list">
        <argument
            android:name="userEmail"
            app:argType="string" />
        <!--<action
            android:id="@+id/action_showDetail"
            app:destination="@id/seriesDetailFragment" />-->
    </fragment>

   <!-- <fragment
        android:id="@+id/seriesDetailFragment"
        android:name="com.facundojaton.moviefiesta.movie_details.MovieDetailsFragment"
        android:label="Movie details"
        tools:layout="@layout/fragment_movie_details">
        <argument
            android:name="selectedMovie"
            app:argType="com.facundojaton.moviefiesta.models.MovieDetails" />
    </fragment>-->

</navigation>